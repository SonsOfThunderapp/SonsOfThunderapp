# HEADER MARK — ETERNAL LOCK (2026-08-23)

Phone-approved. Do not reopen. Do not “improve.”

**Proof:** IMG_7978 — white SONS, gold 3D bolt through the O, red OF THUNDER, HD.

**Source:** `assets/CANONICAL/logo-print-diecut.png`  
**Production slots:** `logo@2x.png` · `logo@3x.png` · `logo@4x.png`

## Law

1. This mark lives in `#main-header` on Home, Brothers, Memories, More.
2. Sticky. Top of the page stack. `z-index: 8000`. Thunder and cards sit under it.
3. True aspect `3000 / 2077`. `object-fit: contain`. Never stretch. Never crop.
4. Never `max-height` cap. Never `overflow: hidden` on `#main-header` or `.logo-wrap`. Never `clip-path` / `contain: paint` on the header.
5. Never AI-redraw. Never swap for a stub SONS-only file.
6. Overlays (tour, auth, sheets) may cover it while open. Pages may not hide it.

Hard fail: header shows SONS without red OF THUNDER. That was the 64–96px clip from `chief1-ghost.css` / `memories-page.css`. Dead. Do not resurrect.
