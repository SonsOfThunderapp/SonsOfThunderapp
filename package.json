{
  "name": "thunder-board",
  "private": true,
  "description": "Sons of Thunder — Thunder Board PWA (static + Netlify Functions)",
  "scripts": {
    "build": "echo \"Thunder Board: static publish (no compile step)\""
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
